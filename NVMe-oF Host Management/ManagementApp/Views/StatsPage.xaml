<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ManagementApp.Views.StatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ManagementApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:localControls="using:ManagementApp.Controls"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical"
                Spacing="12">

        <localControls:AutoPageHeader />

        <Button x:Name="BtnRefresh"
                x:Uid="RefreshDisks"
                Click="BtnRefresh_OnClick"
                IsEnabled="{x:Bind IsConnected}" >
            <SymbolIcon Symbol="Refresh" />
        </Button>

        <Grid RowSpacing="12"
              ColumnSpacing="36">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Uid="NetworkPacketThroughput"
                       Grid.Row="0"
                       Grid.Column="0" />

            <TextBlock x:Uid="NetworkAverageRequestSize"
                       Grid.Row="1"
                       Grid.Column="0" />

            <TextBlock x:Uid="NetworkTotalTransferred"
                       Grid.Row="2"
                       Grid.Column="0" />

            <TextBlock Grid.Row="0"
                       Grid.Column="1">

                <Run Text="{x:Bind ViewModel.PacketsPerSecond}" />
                <Run>pkt/s</Run>
            </TextBlock>

            <TextBlock Grid.Row="1"
                       Grid.Column="1">

                <Run Text="{x:Bind ViewModel.AverageRequestSize}" />
                <Run>B</Run>
            </TextBlock>

            <TextBlock Grid.Row="2"
                       Grid.Column="1">

                <Run Text="{x:Bind ViewModel.TotalDataTransferred}" />
                <Run>B</Run>
            </TextBlock>
        </Grid>
    </StackPanel>
</Page>
